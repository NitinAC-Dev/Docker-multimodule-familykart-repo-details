spring:
  datasource:
    #url: jdbc:mysql://mysql-container:3306/familykartdb enable this when you are using docker module project else use below
    url: jdbc:mysql://localhost:3307/familykartdb
    username: root
    password: root		

#jpa specific properties
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

message: this is Order service profile